const { 
    inquirerMenu,
    pause,
    readInput,
    listOfTaskToDelete,
    confirm,
    showListChecklist,
        } = require('./helpers/inquirer');
const main = async()=> {

    const text = await readInput('Hola: ')  
    
    console.log(texto);
}

main();